import { PhasePlaceholder } from '../../shared/ui/PhasePlaceholder';

export function ControlPanel() {
  return <PhasePlaceholder title="ControlPanel placeholder. E-Stop and joystick are scheduled for Phase 3." />;
}
